const scan = null

função "inserir" {
 "scan" recebe "inserir"
 retorna "valor" 
}

const resultado = null
variante "signo" = +|-|/|*

função "resolver" {
 "resultado" recebe "scan" 
 coleta INT numero, elemento 1n + "signo" + elemento n2
 for ("coleta" = "resultado" + "signo" + elemento (n1 + 2), resolver ++)
 }

console.log ("insira uma operação", $inserir)
wait *operação 
console.log ($resolver)

é possivel fazer algo semelhante com isso para fazer uma calculadora?